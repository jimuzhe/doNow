# DoNow Auth Server 配置
# 复制此文件为 .env 并修改配置

# 安全密钥（生产环境必须更改！运行 python -c "import secrets; print(secrets.token_hex(32))" 生成）
SECRET_KEY=3c66af71f2eb457157e2d43ce7b5e5aeb861288392334bbe651b9f906c4e9494

# JWT 过期时间（小时），默认 30 天
JWT_EXPIRATION_HOURS=720

# MySQL 配置
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=K+^PGXJzSXO41C$+3
MYSQL_DATABASE=donow_auth

# 邮件服务器配置 (SMTP)
MAIL_SERVER=smtp.qq.com
MAIL_PORT=465
MAIL_USE_TLS=false
MAIL_USE_SSL=true
MAIL_USERNAME=3106628438@qq.com
MAIL_PASSWORD=pkjirxozqkuzdejf
MAIL_DEFAULT_SENDER=DoNow App <3106628438@qq.com>

# 前端地址（用于生成邮件中的链接）
# 如果是 App Deep Link，可以是 donow://auth
# 如果是 Web 页面，可以是 https://auth.name666.top
FRONTEND_URL=https://auth.name666.top

# 服务器端口
PORT=5000

# 调试模式（生产环境设为 false）
FLASK_DEBUG=false
